<Page
    x:Class="_19_BindingListaPersonas.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_19_BindingListaPersonas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:_19_BindingListaPersonas.Models.ViewModel"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:VMMainPage></vm:VMMainPage>
    </Page.DataContext>


    <Grid Background="Beige">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <ListView x:Name="listaPersonas" MaxWidth="200" ItemsSource="{Binding ListaPersonas}" SelectedItem="{Binding PersonaSelected, Mode=TwoWay}" Grid.RowSpan="6" Grid.Column="0" HorizontalAlignment="Left" Background="White">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="nombre" Text="{Binding nombre}" Margin="0 0 8 0" />
                        <TextBlock x:Name="apellido" Text="{Binding apellido}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="textNombre" Text="Nombre: " Grid.Column="1" Grid.Row="0" />
        <TextBox x:Name="nombrePersona" Grid.Column="2" Grid.Row="0" Text="{Binding PersonaSelected.nombre}" />

        <TextBlock x:Name="textApellido" Text="Apellido: " Grid.Column="1" Grid.Row="1" />
        <TextBox x:Name="apellidoPersona" Grid.Column="2" Grid.Row="1" Text="{Binding PersonaSelected.apellido}" />

        <TextBlock x:Name="textTelefono" Text="Teléfono: " Grid.Column="1" Grid.Row="2" />
        <TextBox x:Name="telefonoPersona" Grid.Column="2" Grid.Row="2" Text="{Binding PersonaSelected.telefono}" />

        <TextBlock x:Name="textDireccion" Text="Dirección: " Grid.Column="1" Grid.Row="3" />
        <TextBox x:Name="direccionPersona" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="4" Grid.RowSpan="2" Text="{Binding PersonaSelected.direccion}" />

    </Grid>
</Page>
